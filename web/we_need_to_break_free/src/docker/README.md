This folder is the root of the Docker environment to build from.

```bash
docker build . -t <challenge_name>
```
The above command should get the container built from this directory.

*THERE MAYBE SPOILERS HERE*
